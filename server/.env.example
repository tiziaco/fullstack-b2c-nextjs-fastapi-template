# Environment Configuration Example

# Application Settings
APP_ENV=development
PROJECT_NAME="B2C Template Server"
DEBUG=true

# CORS Settings
ALLOWED_ORIGINS='["http://localhost:3000", "http://localhost:8100"]'

# Langfuse Settings
LANGFUSE_PUBLIC_KEY="your-langfuse-public-key"
LANGFUSE_SECRET_KEY="your-langfuse-secret-key"
LANGFUSE_HOST=https://cloud.langfuse.com

# LLM Settings
OPENAI_API_KEY="your-llm-api-key" # e.g. OpenAI API key
DEFAULT_LLM_MODEL=gpt-4o-mini
DEFAULT_LLM_TEMPERATURE=0.2

# Clerk Authentication Settings
CLERK_SECRET_KEY="your-clerk-secret-key"
CLERK_ISSUER="https://your-instance.clerk.accounts.dev"
CLERK_OAUTH_CLIENT_ID="your-clerk-oauth-client-id"
CLERK_AUTHORIZE_URL="https://your-instance.clerk.accounts.dev/oauth/authorize"
CLERK_TOKEN_URL="https://your-instance.clerk.accounts.dev/oauth/token"
CLERK_WEBHOOK_SIGNING_SECRET="your-webhook-signing-secret"

# Database Settings
# For local development: use 'localhost'
# For Docker: docker-compose.dev.yml automatically overrides to 'db'
POSTGRES_HOST=localhost
POSTGRES_DB=mydb
POSTGRES_USER=myuser
# Host port, used when running the server OUTSIDE Docker. Inside Docker the
# server reaches the db container on 5432, which docker-compose.dev.yml pins.
# Override the host binding with POSTGRES_HOST_PORT in the root .env.
POSTGRES_PORT=5434
POSTGRES_PASSWORD=mypassword
POSTGRES_POOL_SIZE=5
POSTGRES_MAX_OVERFLOW=10

# Rate Limiting Settings
RATE_LIMIT_DEFAULT='["1000 per day", "200 per hour"]'
RATE_LIMIT_CHAT='["100 per minute"]'
RATE_LIMIT_CHAT_STREAM='["100 per minute"]'
RATE_LIMIT_MESSAGES='["200 per minute"]'

# Logging
LOG_LEVEL=DEBUG
LOG_FORMAT=console
