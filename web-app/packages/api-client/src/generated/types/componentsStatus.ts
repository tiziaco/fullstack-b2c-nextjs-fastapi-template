/**
 * Generated by orval 🍺
 * Do not edit manually.
 * B2C Template - Server
 * A production-ready FastAPI service with LangGraph and Clerk authentication, for consumer applications.
 * OpenAPI spec version: 0.1.0
 */
import type { ComponentsStatusAgents } from "./componentsStatusAgents"
import type { DatabaseStatus } from "./databaseStatus"

/**
 * Per-component readiness detail.
 *
 * `agents` is always a mapping of agent name to status — never a bare string,
 * and never wrapped in an envelope. The "agents were never initialised"
 * condition is carried by `agents_error`, so the value's shape does not
 * depend on the outcome.
 */
export interface ComponentsStatus {
  /** Agent name to readiness status */
  agents?: ComponentsStatusAgents
  /** Set when the agent registry was never initialised */
  agents_error?: string | null
  /** Every registered agent is ready */
  agents_healthy: boolean
  /** The API itself is serving */
  api: "healthy"
  /** Database component */
  database: DatabaseStatus
}
